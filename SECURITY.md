# Security Policy

## Reporting a Vulnerability

إذا اكتشفت ثغرة أمنية في هذا المشروع، نقدر إبلاغنا بها بطريقة مسؤولة.

### كيفية الإبلاغ

**الرجاء عدم** فتح issue عام على GitHub للثغرات الأمنية.

بدلاً من ذلك:

1. أرسل بريد إلكتروني إلى: [security@example.com]
2. ضع في الموضوع: "Security Vulnerability Report"
3. صف الثغرة بالتفصيل
4. أرفق أي معلومات ضرورية

### ما نحتاجه منك

عند الإبلاغ عن ثغرة أمنية، يرجى تضمين:

- **وصف الثغرة**: ما هي المشكلة؟
- **الموقع**: أين توجد في الكود؟
- **التأثير المحتمل**: ما الضرر المحتمل؟
- **خطوات إعادة الإنتاج**: كيف يمكن إعادة المشكلة؟
- **حل مقترح** (إن أمكن)

### ما يمكنك توقعه

- **الرد الأولي**: خلال 48 ساعة
- **التحديثات**: كل 5-7 أيام حتى تُحل المشكلة
- **الإصلاح**: حسب شدة الثغرة (عادة 7-30 يوم)
- **الشكر**: سيتم ذكر اسمك في قائمة الشكر (إذا رغبت)

## الثغرات المقبولة

نهتم بالإبلاغات عن:

- ✅ SQL Injection
- ✅ XSS (Cross-Site Scripting)
- ✅ CSRF (Cross-Site Request Forgery)
- ✅ Authentication/Authorization issues
- ✅ Data exposure
- ✅ Server-Side Request Forgery (SSRF)
- ✅ Remote Code Execution (RCE)

## خارج النطاق

لا نقبل تقارير عن:

- ❌ Social engineering
- ❌ Physical attacks
- ❌ Denial of Service (DoS)
- ❌ Issues in dependencies (report to original author)
- ❌ Self-XSS
- ❌ Missing security headers without proof of exploitation

## أفضل الممارسات الأمنية

### للمطورين

#### 1. Environment Variables

\`\`\`bash

# ❌ لا تكشف أبداً المفاتيح السرية

AUTH_SECRET=super-secret-key

# ✅ استخدم مفاتيح معقدة

AUTH_SECRET=$(openssl rand -base64 32)
\`\`\`

#### 2. Database Queries

\`\`\`typescript
// ❌ سيء - معرض لـ SQL Injection
const users = await db.query(\`SELECT \* FROM users WHERE id = \${id}\`);

// ✅ جيد - استخدم parameterized queries
const users = await db.query('SELECT \* FROM users WHERE id = $1', [id]);
\`\`\`

#### 3. Password Handling

\`\`\`typescript
// ❌ لا تخزن كلمات المرور بنص صريح
user.password = password;

// ✅ استخدم bcrypt مع salt
import \* as bcrypt from 'bcrypt';
const hash = await bcrypt.hash(password, 10);
\`\`\`

#### 4. JWT Secrets

\`\`\`typescript
// ❌ مفاتيح ضعيفة
JWT_SECRET=secret123

// ✅ مفاتيح قوية وطويلة
JWT_SECRET=a8f5f167f44f4964e6c998dee827110c
\`\`\`

#### 5. CORS Configuration

\`\`\`typescript
// ❌ خطير - يسمح لأي نطاق
app.enableCors({ origin: '\*' });

// ✅ محدد - فقط النطاقات الموثوقة
app.enableCors({ origin: process.env.CLIENT_URL });
\`\`\`

### للمستخدمين

1. **استخدم كلمات مرور قوية**
   - على الأقل 12 حرف
   - مزيج من أحرف كبيرة وصغيرة وأرقام ورموز

2. **لا تشارك بيانات الدخول**
   - لا ترسل كلمة المرور عبر البريد الإلكتروني
   - لا تحفظها في ملفات نصية

3. **حافظ على تحديث المتصفح**
   - استخدم أحدث إصدار من المتصفح

4. **تحقق من HTTPS**
   - تأكد من أن الموقع يستخدم HTTPS

## Security Checklist

### قبل الإنتاج

- [ ] تغيير جميع المفاتيح السرية
- [ ] تفعيل HTTPS
- [ ] تحديد CORS بشكل صحيح
- [ ] تفعيل rate limiting
- [ ] إضافة helmet للـ security headers
- [ ] تفعيل logging للـ security events
- [ ] فحص Dependencies للثغرات
- [ ] إزالة console.logs من الإنتاج
- [ ] تفعيل CSP (Content Security Policy)
- [ ] Sanitize user inputs

### أدوات مفيدة

\`\`\`bash

# فحص Dependencies للثغرات

npm audit
npm audit fix

# استخدام Snyk

npx snyk test

# استخدام OWASP Dependency Check

npm install -g owasp-dependency-checker
\`\`\`

## الإصدارات المدعومة

| Version | Supported          |
| ------- | ------------------ |
| 1.x.x   | :white_check_mark: |
| < 1.0   | :x:                |

## شكر وتقدير

نشكر الباحثين الأمنيين التالية أسماؤهم لإبلاغنا عن ثغرات:

<!-- سيتم تحديث القائمة -->

---

شكراً لمساعدتك في الحفاظ على أمان المشروع! 🔒
